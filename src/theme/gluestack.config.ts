/** Bridge Masjidy tokens to Gluestack v3 where needed — extend when adding primitives */

export const gluestackThemeBridge = {
  note: 'Gluestack v3 uses NativeWind + provider config; customize alongside tailwind.config.js',
} as const;
