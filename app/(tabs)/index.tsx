import { Text, View } from 'react-native';

export default function MosquesTabScreen() {
  return (
    <View className="flex-1 items-center justify-center p-5">
      <Text>(tabs)/index — Mosques</Text>
    </View>
  );
}
